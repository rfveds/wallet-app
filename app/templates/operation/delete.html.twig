{% include 'default/_form.html.twig' with {
    page_title: 'title.operation_delete'|trans({'%id%': operation.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'operation_index',
} %}
