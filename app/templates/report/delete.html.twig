{% include 'default/_form.html.twig' with {
    page_title: 'title.report_delete'|trans({'%id%': report.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'report_index',
} %}
